{
  "name": "veryl-sccache-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only anonymous gateway to the veryl sccache R2 cache (for fork-PR cache reads)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
