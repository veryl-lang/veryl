pub proto module synchronizer #(
    param WIDTH : u32 = 8,
    param STAGES: u32 = 2,
) (
    i_clk: input  'd clock       ,
    i_rst: input  'd reset       ,
    i_d  : input  's logic<WIDTH>,
    o_d  : output 'd logic<WIDTH>,
);
