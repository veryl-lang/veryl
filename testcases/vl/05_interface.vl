// interface declaration
interface Interface05 #(
    // interface parameter
    parameter  a  : u32  = 1,
    parameter  aa : u32  = 1,
    localparam aaa: u32  = 1,
) {
    // localparam declaration
    localparam b : u32  = 1;
    localparam bb: u64  = 1;

    // let declaration
    let c  : logic         ;
    let cc : logic [10]    ;
    let ccc: bit   [10][10];

    // modport declaration
    modport d {
        c  : input ,
        cc : output,
        ccc: inout ,
    }
}
